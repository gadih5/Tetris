#pragma once
#include <vector>
#include <map>

// Movement Enum
enum Movement
{
	Up = 0,
	Down = 1,
	Left = 2,
	Right = 3,
	RotateClockwise = 4,
	RotateCounterClockwise = 5,
	Drop = 6
};

enum SpecialKeys
{
	ESC = 27
};
